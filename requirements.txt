Flask==3.0.0
requests==2.31.0
Pillow==10.2.0
tkinter==0.1.0  # Não é instalável via pip, mas mantido aqui para referência
sqlite3==0.1.0  # Não é instalável via pip, mas mantido aqui para referência
cryptography==42.0.5
